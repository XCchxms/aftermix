{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions de la fenêtre principale. La lecture des clips locaux n'apparaît pas ici : le protocole asset se restreint par le `scope` déclaré dans tauri.conf.json, pas par une permission de capacité.",
  "windows": ["main", "overlay"],
  "permissions": ["core:default", "global-shortcut:default", "notification:default"]
}
